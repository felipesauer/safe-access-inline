import{_ as e,o as s,c as o,ag as n}from"./chunks/framework.DXzMPhrv.js";const u=JSON.parse('{"title":"Arquitetura","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"pt-br/guide/architecture.md","filePath":"pt-br/guide/architecture.md","lastUpdated":1773692034000}'),i={name:"pt-br/guide/architecture.md"};function r(t,a,d,l,c,p){return s(),o("div",null,[...a[0]||(a[0]=[n(`<h1 id="arquitetura" tabindex="-1">Arquitetura <a class="header-anchor" href="#arquitetura" aria-label="Permalink to &quot;Arquitetura&quot;">​</a></h1><h2 id="indice" tabindex="-1">Índice <a class="header-anchor" href="#indice" aria-label="Permalink to &quot;Índice&quot;">​</a></h2><ul><li><a href="#arquitetura">Arquitetura</a><ul><li><a href="#indice">Índice</a></li><li><a href="#visao-geral">Visão Geral</a></li><li><a href="#principios-de-design">Princípios de Design</a></li><li><a href="#diagrama-de-componentes">Diagrama de Componentes</a></li><li><a href="#sistema-de-plugins">Sistema de Plugins</a><ul><li><a href="#contratos">Contratos</a></li><li><a href="#pluginregistry">PluginRegistry</a></li><li><a href="#comportamento-php-vs-js">Comportamento PHP vs JS</a></li></ul></li><li><a href="#fluxo-de-dados">Fluxo de Dados</a></li><li><a href="#motor-dotnotationparser">Motor DotNotationParser</a></li><li><a href="#padrao-de-imutabilidade">Padrão de Imutabilidade</a></li><li><a href="#typedetector">TypeDetector</a></li><li><a href="#estrutura-do-monorepo">Estrutura do Monorepo</a></li><li><a href="#arquitetura-de-seguranca">Arquitetura de Segurança</a></li><li><a href="#i-o-carregamento-de-arquivos">I/O &amp; Carregamento de Arquivos</a></li><li><a href="#validacao-de-schema">Validação de Schema</a></li><li><a href="#sistema-de-auditoria">Sistema de Auditoria</a></li><li><a href="#pacote-cli">Pacote CLI</a></li><li><a href="#integracoes-com-frameworks">Integrações com Frameworks</a></li><li><a href="#registros-de-decisao-de-arquitetura">Registros de Decisão de Arquitetura</a><ul><li><a href="#adr-1-set-remove-usam-clone-em-vez-de-static-from">ADR-1: <code>set()</code> / <code>remove()</code> usam <code>clone</code> em vez de <code>static::from()</code></a></li><li><a href="#adr-2-js-toxml-toyaml-totoml-via-bibliotecas-reais-plugin-override">ADR-2: JS <code>toXml()</code> / <code>toYaml()</code> / <code>toToml()</code> via Bibliotecas Reais + Plugin Override</a></li><li><a href="#adr-3-dependencias-reais-para-yaml-toml-pluginregistry-para-override">ADR-3: Dependências Reais para YAML/TOML + PluginRegistry para Override</a></li></ul></li></ul></li></ul><h2 id="visao-geral" tabindex="-1">Visão Geral <a class="header-anchor" href="#visao-geral" aria-label="Permalink to &quot;Visão Geral&quot;">​</a></h2><p>safe-access-inline é uma biblioteca de acesso a dados agnóstica de formato que fornece uma única API para ler, escrever e transformar estruturas de dados profundamente aninhadas com segurança. Segue o padrão <strong>Facade</strong> com um sistema de accessors plugáveis e um <strong>Plugin Registry</strong> extensível para parsing e serialização de formatos.</p><h2 id="principios-de-design" tabindex="-1">Princípios de Design <a class="header-anchor" href="#principios-de-design" aria-label="Permalink to &quot;Princípios de Design&quot;">​</a></h2><ol><li><strong>Zero Surpresas</strong> — <code>get()</code> nunca lança exceções. Caminhos não encontrados retornam um valor padrão.</li><li><strong>Agnóstico de Formato</strong> — A mesma API funciona de forma idêntica em todos os formatos suportados.</li><li><strong>Imutabilidade</strong> — <code>set()</code> e <code>remove()</code> sempre retornam novas instâncias; o original nunca é modificado.</li><li><strong>Dependências Reais para Formatos Complexos</strong> — YAML e TOML usam bibliotecas reais (<code>js-yaml</code>/<code>smol-toml</code> em JS, <code>symfony/yaml</code>/<code>devium/toml</code> em PHP) como dependências. O Sistema de Plugins fornece capacidade opcional de override.</li><li><strong>Extensibilidade</strong> — Accessors customizados via <code>SafeAccess::extend()</code>, parsers e serializers customizados via <code>PluginRegistry</code>.</li></ol><h2 id="diagrama-de-componentes" tabindex="-1">Diagrama de Componentes <a class="header-anchor" href="#diagrama-de-componentes" aria-label="Permalink to &quot;Diagrama de Componentes&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────────────────┐</span></span>
<span class="line"><span>│       SafeAccess Facade      │  ← Ponto de entrada estático</span></span>
<span class="line"><span>│  fromArray / fromJson / ...  │</span></span>
<span class="line"><span>│  detect / extend / custom    │</span></span>
<span class="line"><span>└──────────┬───────────────────┘</span></span>
<span class="line"><span>           │ cria</span></span>
<span class="line"><span>           ▼</span></span>
<span class="line"><span>┌──────────────────────────────┐</span></span>
<span class="line"><span>│     AbstractAccessor         │  ← Classe base (toda a lógica)</span></span>
<span class="line"><span>│  get / set / remove / has    │</span></span>
<span class="line"><span>│  toArray / toJson / toXml    │</span></span>
<span class="line"><span>│  toYaml / toToml / transform │</span></span>
<span class="line"><span>│  type / count / keys / all   │</span></span>
<span class="line"><span>│  push / pop / shift / insert │  ← Operações de array</span></span>
<span class="line"><span>│  diff / applyPatch           │  ← JSON Patch (RFC 6902)</span></span>
<span class="line"><span>│  masked / validate           │  ← Segurança &amp; schema</span></span>
<span class="line"><span>└──────────┬───────────────────┘</span></span>
<span class="line"><span>           │</span></span>
<span class="line"><span>     ┌─────┴─────────────────────┐</span></span>
<span class="line"><span>     │ delega resolução          │ delega serialização</span></span>
<span class="line"><span>     │ de caminho para           │ &amp; parsing para</span></span>
<span class="line"><span>     ▼                           ▼</span></span>
<span class="line"><span>┌────────────────────┐   ┌────────────────────────┐</span></span>
<span class="line"><span>│  DotNotationParser │   │    PluginRegistry       │</span></span>
<span class="line"><span>│  get / has / set   │   │  registerParser()       │</span></span>
<span class="line"><span>│  remove / parseKeys│   │  registerSerializer()   │</span></span>
<span class="line"><span>│  buildPath         │   │  getParser / has / get  │</span></span>
<span class="line"><span>└────────────────────┘   └────────┬───────────────┘</span></span>
<span class="line"><span>                                  │ armazena</span></span>
<span class="line"><span>                     ┌────────────┼────────────────┐</span></span>
<span class="line"><span>                     ▼            ▼                ▼</span></span>
<span class="line"><span>              ┌───────────┐ ┌──────────────┐ ┌──────────┐</span></span>
<span class="line"><span>              │  Parsers   │ │ Serializers  │ │ Custom   │</span></span>
<span class="line"><span>              │  yaml,toml │ │ yaml,xml,... │ │ plugins  │</span></span>
<span class="line"><span>              └───────────┘ └──────────────┘ └──────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Accessors Concretos (estendem AbstractAccessor):</span></span>
<span class="line"><span>┌──────────┬──────────┬──────────┐</span></span>
<span class="line"><span>│  Array   │  Object  │  JSON    │</span></span>
<span class="line"><span>│  XML     │  YAML    │  TOML    │</span></span>
<span class="line"><span>│  INI     │  CSV     │  ENV     │</span></span>
<span class="line"><span>│  NDJSON  │          │          │</span></span>
<span class="line"><span>└──────────┴──────────┴──────────┘</span></span>
<span class="line"><span>Cada um implementa apenas: parse(raw) → array</span></span>
<span class="line"><span>(YAML/TOML usam bibliotecas reais por padrão, com override opcional via PluginRegistry)</span></span></code></pre></div><h2 id="sistema-de-plugins" tabindex="-1">Sistema de Plugins <a class="header-anchor" href="#sistema-de-plugins" aria-label="Permalink to &quot;Sistema de Plugins&quot;">​</a></h2><p>O Sistema de Plugins fornece capacidade de <strong>override opcional</strong> para parsing e serialização de formatos específicos. YAML e TOML usam bibliotecas reais por padrão — plugins permitem que usuários substituam por implementações alternativas.</p><h3 id="contratos" tabindex="-1">Contratos <a class="header-anchor" href="#contratos" aria-label="Permalink to &quot;Contratos&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌───────────────────────────┐     ┌───────────────────────────────┐</span></span>
<span class="line"><span>│  ParserPluginInterface    │     │  SerializerPluginInterface    │</span></span>
<span class="line"><span>│  parse(string): array     │     │  serialize(array): string     │</span></span>
<span class="line"><span>└───────────────────────────┘     └───────────────────────────────┘</span></span></code></pre></div><ul><li><strong><code>ParserPluginInterface</code></strong> — recebe uma string bruta (ex: texto YAML), retorna um array associativo normalizado. Lança <code>InvalidFormatException</code> para input malformado.</li><li><strong><code>SerializerPluginInterface</code></strong> — recebe um array normalizado, retorna uma string formatada (ex: texto YAML).</li></ul><h3 id="pluginregistry" tabindex="-1">PluginRegistry <a class="header-anchor" href="#pluginregistry" aria-label="Permalink to &quot;PluginRegistry&quot;">​</a></h3><p>Um registro estático que mapeia nomes de formato (ex: <code>&#39;yaml&#39;</code>, <code>&#39;toml&#39;</code>) para implementações de parser e serializer.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PluginRegistry::registerParser(&#39;yaml&#39;, new SymfonyYamlParser());</span></span>
<span class="line"><span>PluginRegistry::registerSerializer(&#39;yaml&#39;, new SymfonyYamlSerializer());</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Accessors consultam o registro:</span></span>
<span class="line"><span>// YamlAccessor::parse() → PluginRegistry::getParser(&#39;yaml&#39;)-&gt;parse($raw)</span></span>
<span class="line"><span>// $accessor-&gt;toYaml()   → PluginRegistry::getSerializer(&#39;yaml&#39;)-&gt;serialize($data)</span></span>
<span class="line"><span>// $accessor-&gt;transform(&#39;yaml&#39;) → mesmo que toYaml()</span></span></code></pre></div><h3 id="comportamento-php-vs-js" tabindex="-1">Comportamento PHP vs JS <a class="header-anchor" href="#comportamento-php-vs-js" aria-label="Permalink to &quot;Comportamento PHP vs JS&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspecto</th><th>PHP</th><th>JS/TS</th></tr></thead><tbody><tr><td>Parsing YAML/TOML</td><td>Biblioteca real por padrão (<code>ext-yaml</code> ou <code>symfony/yaml</code> para YAML, <code>devium/toml</code> para TOML); plugin <strong>opcional</strong> (overrides)</td><td>Biblioteca real por padrão (<code>js-yaml</code>, <code>smol-toml</code>); plugin <strong>opcional</strong> (overrides)</td></tr><tr><td>Serialização (<code>toYaml</code>, <code>toToml</code>, <code>toXml</code>, <code>transform</code>)</td><td>Plugin override → fallback para <code>ext-yaml</code>/biblioteca real (com fallback <code>SimpleXMLElement</code> para XML)</td><td>Biblioteca real por padrão para YAML/TOML; plugin necessário para XML</td></tr><tr><td>Plugins incluídos</td><td>6 plugins (SymfonyYamlParser, SymfonyYamlSerializer, NativeYamlParser, NativeYamlSerializer, DeviumTomlParser, DeviumTomlSerializer)</td><td>4 plugins (JsYamlParser, JsYamlSerializer, SmolTomlParser, SmolTomlSerializer)</td></tr></tbody></table><h2 id="fluxo-de-dados" tabindex="-1">Fluxo de Dados <a class="header-anchor" href="#fluxo-de-dados" aria-label="Permalink to &quot;Fluxo de Dados&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Input (string/array/object)</span></span>
<span class="line"><span>  → Accessor::from(data)</span></span>
<span class="line"><span>    → Construtor: raw = data, data = parse(raw)</span></span>
<span class="line"><span>      → Para a maioria dos formatos: parsing específico do accessor (JSON.parse, XML parse, etc.)</span></span>
<span class="line"><span>      → Para YAML (PHP): Plugin override → ext-yaml (yaml_parse) → fallback Symfony\\Yaml</span></span>
<span class="line"><span>      → Para TOML (PHP): Plugin override → Devium\\Toml padrão</span></span>
<span class="line"><span>      → Para YAML/TOML (JS): Plugin override → biblioteca real padrão (js-yaml / smol-toml)</span></span>
<span class="line"><span>  → Array normalizado armazenado em $data / this.data</span></span>
<span class="line"><span>  → Todas operações de leitura (get/set/has/etc.) operam sobre data via DotNotationParser</span></span>
<span class="line"><span>  → Métodos de transformação:</span></span>
<span class="line"><span>      → toArray() / toJson() / toObject() — embutidos, sempre disponíveis</span></span>
<span class="line"><span>      → toXml() — embutido em PHP (SimpleXMLElement), baseado em plugin em JS</span></span>
<span class="line"><span>      → toYaml() — Plugin override → ext-yaml (yaml_emit) → fallback symfony/yaml em PHP; js-yaml padrão em JS</span></span>
<span class="line"><span>      → toToml() — Plugin override → devium/toml padrão em PHP; smol-toml padrão em JS</span></span>
<span class="line"><span>      → transform(format) — sempre delega para PluginRegistry::getSerializer(format)</span></span></code></pre></div><h2 id="motor-dotnotationparser" tabindex="-1">Motor DotNotationParser <a class="header-anchor" href="#motor-dotnotationparser" aria-label="Permalink to &quot;Motor DotNotationParser&quot;">​</a></h2><p>O parser resolve caminhos como <code>user.profile.name</code> contra estruturas de dados aninhadas.</p><p><strong>Sintaxe de caminhos suportada:</strong></p><ul><li><code>name</code> — acesso a chave simples</li><li><code>user.profile.name</code> — acesso aninhado</li><li><code>items.0.title</code> — acesso por índice numérico</li><li><code>matrix[0][1]</code> — notação de colchetes (convertida para notação de ponto)</li><li><code>users.*.name</code> — wildcard (retorna array de todos os valores correspondentes)</li><li><code>config\\.db.host</code> — ponto escapado (ponto literal no nome da chave)</li></ul><p><strong>Algoritmo de resolução:</strong></p><ol><li>Analisa o caminho em segmentos de chave via <code>parseKeys()</code></li><li>Percorre a estrutura de dados segmento por segmento</li><li>No wildcard <code>*</code>: itera todos os filhos, resolve recursivamente o caminho restante</li><li>No ponto escapado: trata como nome literal da chave</li><li>Retorna o valor padrão se qualquer segmento não for encontrado</li></ol><h2 id="padrao-de-imutabilidade" tabindex="-1">Padrão de Imutabilidade <a class="header-anchor" href="#padrao-de-imutabilidade" aria-label="Permalink to &quot;Padrão de Imutabilidade&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$original = SafeAccess::fromJson(&#39;{&quot;a&quot;: 1}&#39;);</span></span>
<span class="line"><span>$modified = $original-&gt;set(&#39;b&#39;, 2);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// $original-&gt;data = [&#39;a&#39; =&gt; 1]     ← inalterado</span></span>
<span class="line"><span>// $modified-&gt;data = [&#39;a&#39; =&gt; 1, &#39;b&#39; =&gt; 2]  ← nova instância</span></span></code></pre></div><p>Implementação:</p><ul><li>PHP: <code>clone $this</code> + atualiza <code>$data</code></li><li>JS: método <code>clone(newData)</code> cria nova instância com dados modificados (via <code>structuredClone</code>)</li></ul><h2 id="typedetector" tabindex="-1">TypeDetector <a class="header-anchor" href="#typedetector" aria-label="Permalink to &quot;TypeDetector&quot;">​</a></h2><p>Prioridade de auto-detecção (primeiro match vence):</p><ol><li><strong>Array</strong> → <code>ArrayAccessor</code></li><li><strong>SimpleXMLElement</strong> (apenas PHP) → <code>XmlAccessor</code></li><li><strong>Object</strong> → <code>ObjectAccessor</code></li><li><strong>String JSON</strong> (<code>{</code> ou <code>[</code>) → <code>JsonAccessor</code></li><li><strong>String NDJSON</strong> (múltiplas linhas <code>{...}</code>) → <code>NdjsonAccessor</code></li><li><strong>String XML</strong> (<code>&lt;?xml</code> ou <code>&lt;</code>) → <code>XmlAccessor</code></li><li><strong>String YAML</strong> (contém pares <code>: </code> ou front-matter <code>---</code>) → <code>YamlAccessor</code></li><li><strong>String INI</strong> (tem <code>[seção]</code> ou <code>chave = valor</code>) → <code>IniAccessor</code></li><li><strong>String ENV</strong> (padrão <code>CHAVE=VALOR</code>) → <code>EnvAccessor</code></li><li><strong>Não suportado</strong> → lança <code>UnsupportedTypeError</code> / <code>UnsupportedTypeException</code></li></ol><blockquote><p><strong>Limitações:</strong> TOML e CSV não são auto-detectados devido à ambiguidade de formato. A heurística YAML (padrão <code>chave:</code> sem <code>=</code>) pode produzir falsos positivos para strings que não são YAML. Sempre prefira métodos factory explícitos (ex: <code>fromYaml()</code>, <code>fromToml()</code>) para inputs ambíguos.</p></blockquote><h2 id="estrutura-do-monorepo" tabindex="-1">Estrutura do Monorepo <a class="header-anchor" href="#estrutura-do-monorepo" aria-label="Permalink to &quot;Estrutura do Monorepo&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>safe-access-inline/</span></span>
<span class="line"><span>├── packages/</span></span>
<span class="line"><span>│   ├── php/                 # Pacote Composer</span></span>
<span class="line"><span>│   │   ├── src/</span></span>
<span class="line"><span>│   │   │   ├── Accessors/   # 10 accessors de formato (incl. NDJSON)</span></span>
<span class="line"><span>│   │   │   ├── Contracts/   # Interfaces (incl. ParserPlugin, SerializerPlugin, SchemaAdapter)</span></span>
<span class="line"><span>│   │   │   ├── Core/        # AbstractAccessor, DotNotationParser, TypeDetector, PluginRegistry, SchemaRegistry, JsonPatch, IoLoader, FileWatcher, DeepMerger</span></span>
<span class="line"><span>│   │   │   ├── Enums/       # AccessorFormat enum</span></span>
<span class="line"><span>│   │   │   ├── Exceptions/  # Hierarquia de exceções (incl. SecurityException, SchemaValidationException, ReadonlyViolationException)</span></span>
<span class="line"><span>│   │   │   ├── Integrations/# LaravelServiceProvider, SymfonyIntegration</span></span>
<span class="line"><span>│   │   │   ├── Plugins/     # Plugins incluídos (SymfonyYaml*, NativeYaml*, DeviumToml*)</span></span>
<span class="line"><span>│   │   │   ├── Security/    # SecurityPolicy, SecurityOptions, SecurityGuard, CsvSanitizer, DataMasker, AuditLogger</span></span>
<span class="line"><span>│   │   │   ├── Traits/      # HasFactory, HasTransformations, HasWildcardSupport</span></span>
<span class="line"><span>│   │   │   └── SafeAccess.php</span></span>
<span class="line"><span>│   │   └── tests/</span></span>
<span class="line"><span>│   │       ├── Unit/        # Testes unitários mock-based</span></span>
<span class="line"><span>│   │       └── Integration/ # Testes de integração com parsers reais</span></span>
<span class="line"><span>│   ├── js/                  # Pacote npm</span></span>
<span class="line"><span>│   │   ├── src/</span></span>
<span class="line"><span>│   │   │   ├── accessors/   # 10 accessors de formato (incl. NDJSON)</span></span>
<span class="line"><span>│   │   │   ├── contracts/   # Interfaces TypeScript</span></span>
<span class="line"><span>│   │   │   ├── core/        # AbstractAccessor, DotNotationParser, TypeDetector, PluginRegistry, SchemaRegistry, JsonPatch, IoLoader, FileWatcher, DeepMerger, AuditLogger</span></span>
<span class="line"><span>│   │   │   ├── exceptions/  # Hierarquia de erros (incl. SecurityError, SchemaValidationError, ReadonlyViolationError)</span></span>
<span class="line"><span>│   │   │   ├── integrations/# Módulo NestJS, plugin Vite</span></span>
<span class="line"><span>│   │   │   ├── plugins/     # Plugins incluídos (JsYaml*, SmolToml*)</span></span>
<span class="line"><span>│   │   │   ├── types/       # DeepPaths, ValueAtPath utility types</span></span>
<span class="line"><span>│   │   │   ├── safe-access.ts</span></span>
<span class="line"><span>│   │   │   └── index.ts     # Barrel export</span></span>
<span class="line"><span>│   │   └── tests/</span></span>
<span class="line"><span>│   │       ├── unit/        # Testes unitários mock-based</span></span>
<span class="line"><span>│   │       └── integration/ # Testes de pipeline cross-format</span></span>
<span class="line"><span>│   └── cli/                 # Pacote CLI (@safe-access-inline/cli)</span></span>
<span class="line"><span>│       ├── src/cli.ts       # Ponto de entrada CLI (get, set, remove, transform, diff, mask, layer, keys, type, has, count)</span></span>
<span class="line"><span>│       └── tests/</span></span>
<span class="line"><span>├── docs/                    # Documentação (Jekyll, English + pt-BR)</span></span>
<span class="line"><span>├── .github/workflows/       # CI/CD</span></span>
<span class="line"><span>├── CHANGELOG.md</span></span>
<span class="line"><span>├── CODE_OF_CONDUCT.md</span></span>
<span class="line"><span>├── CONTRIBUTING.md</span></span>
<span class="line"><span>├── LICENSE</span></span>
<span class="line"><span>├── SECURITY.md</span></span>
<span class="line"><span>└── README.md</span></span></code></pre></div><h2 id="arquitetura-de-seguranca" tabindex="-1">Arquitetura de Segurança <a class="header-anchor" href="#arquitetura-de-seguranca" aria-label="Permalink to &quot;Arquitetura de Segurança&quot;">​</a></h2><p>O módulo de segurança fornece defesa em profundidade para processamento de dados:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌─────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│                    SecurityPolicy                       │</span></span>
<span class="line"><span>│  maxDepth, maxPayloadBytes, maxKeys, allowedDirs,       │</span></span>
<span class="line"><span>│  url options, csvMode, maskPatterns                     │</span></span>
<span class="line"><span>└──────────┬──────────────────────────────────────────────┘</span></span>
<span class="line"><span>           │ delega para</span></span>
<span class="line"><span>     ┌─────┴─────────┬──────────────┬────────────────┐</span></span>
<span class="line"><span>     ▼               ▼              ▼                ▼</span></span>
<span class="line"><span>┌──────────┐  ┌──────────────┐ ┌───────────┐  ┌───────────┐</span></span>
<span class="line"><span>│ Security │  │    IoLoader   │ │   CSV     │  │   Data    │</span></span>
<span class="line"><span>│ Options  │  │  path/URL    │ │ Sanitizer │  │  Masker   │</span></span>
<span class="line"><span>│ (limits) │  │  validation  │ │           │  │           │</span></span>
<span class="line"><span>└──────────┘  └──────────────┘ └───────────┘  └───────────┘</span></span></code></pre></div><ul><li><strong>SecurityPolicy</strong> — Agregado imutável de todas as configurações de segurança. Suporta <code>merge()</code> para criar políticas derivadas.</li><li><strong>SecurityOptions</strong> — Métodos estáticos de asserção para limites de tamanho de payload, contagem de chaves e profundidade de aninhamento.</li><li><strong>SecurityGuard</strong> — Bloqueia chaves de prototype pollution (<code>__proto__</code>, <code>constructor</code>, <code>prototype</code>, <code>__defineGetter__</code>, <code>__defineSetter__</code>, <code>__lookupGetter__</code>, <code>__lookupSetter__</code>, <code>valueOf</code>, <code>toString</code>, <code>hasOwnProperty</code>, <code>isPrototypeOf</code>). Sanitiza objetos recursivamente.</li><li><strong>IoLoader</strong> — Proteção contra path-traversal para leitura de arquivos. Proteção contra SSRF para fetch de URLs (bloqueia IPs privados, metadata de cloud, exige HTTPS).</li><li><strong>CsvSanitizer</strong> — Protege contra ataques de CSV injection com modos configuráveis (none, prefix, strip, error).</li><li><strong>DataMasker</strong> — Substitui valores sensíveis (password, token, secret, etc.) por <code>[REDACTED]</code>. Suporta padrões customizados de glob/regex.</li></ul><h2 id="i-o-carregamento-de-arquivos" tabindex="-1">I/O &amp; Carregamento de Arquivos <a class="header-anchor" href="#i-o-carregamento-de-arquivos" aria-label="Permalink to &quot;I/O &amp; Carregamento de Arquivos&quot;">​</a></h2><p>Carregamento de arquivos e URLs segue um pipeline seguro:</p><ol><li><strong>Validação de caminho</strong> — O caminho resolvido deve estar dentro de <code>allowedDirs</code> (se especificado)</li><li><strong>Detecção de formato</strong> — Baseada em extensão (<code>resolveFormatFromExtension</code>)</li><li><strong>Leitura de conteúdo</strong> — Sistema de arquivos ou fetch HTTPS</li><li><strong>Emissão de auditoria</strong> — Evento <code>file.read</code> ou <code>url.fetch</code></li><li><strong>Criação do accessor</strong> — Delegado ao método <code>SafeAccess.from*()</code> apropriado</li></ol><p>File watching usa polling (<code>FileWatcher</code>) — verifica mtime em intervalos configuráveis. Retorna uma função stop para cleanup.</p><p>Configuração em camadas (<code>layer()</code>, <code>layerFiles()</code>) realiza deep-merge de múltiplas fontes com semântica last-wins.</p><h2 id="validacao-de-schema" tabindex="-1">Validação de Schema <a class="header-anchor" href="#validacao-de-schema" aria-label="Permalink to &quot;Validação de Schema&quot;">​</a></h2><p>A validação de schema usa o padrão <strong>Adapter</strong> para permanecer agnóstica de biblioteca:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌───────────────────┐     ┌────────────────────────┐</span></span>
<span class="line"><span>│  SchemaRegistry   │────▶│  SchemaAdapterInterface │</span></span>
<span class="line"><span>│  default adapter  │     │  validate(data, schema) │</span></span>
<span class="line"><span>└───────────────────┘     └────────────┬───────────┘</span></span>
<span class="line"><span>                                       │ retorna</span></span>
<span class="line"><span>                                       ▼</span></span>
<span class="line"><span>                          ┌────────────────────────┐</span></span>
<span class="line"><span>                          │ SchemaValidationResult  │</span></span>
<span class="line"><span>                          │  valid: bool            │</span></span>
<span class="line"><span>                          │  errors: Issue[]        │</span></span>
<span class="line"><span>                          └────────────────────────┘</span></span></code></pre></div><p>Usuários implementam <code>SchemaAdapterInterface</code> com sua biblioteca de validação preferida (Zod, Joi, JSON Schema, etc.) e registram via <code>SchemaRegistry.setDefaultAdapter()</code>.</p><h2 id="sistema-de-auditoria" tabindex="-1">Sistema de Auditoria <a class="header-anchor" href="#sistema-de-auditoria" aria-label="Permalink to &quot;Sistema de Auditoria&quot;">​</a></h2><p>O sistema de auditoria fornece observabilidade para operações relevantes à segurança:</p><ul><li><strong>Tipos de evento:</strong> <code>file.read</code>, <code>file.watch</code>, <code>url.fetch</code>, <code>security.violation</code>, <code>data.mask</code>, <code>data.freeze</code>, <code>schema.validate</code></li><li><strong>Assinatura:</strong> <code>SafeAccess.onAudit(listener)</code> retorna uma função de unsubscribe</li><li><strong>Emissão:</strong> Interna — disparada automaticamente por IoLoader, DataMasker, validação de schema, etc.</li><li><strong>Design:</strong> Padrão pub/sub. Listeners são síncronos. Eventos incluem campos <code>type</code>, <code>timestamp</code> e <code>detail</code>.</li></ul><h2 id="pacote-cli" tabindex="-1">Pacote CLI <a class="header-anchor" href="#pacote-cli" aria-label="Permalink to &quot;Pacote CLI&quot;">​</a></h2><p>O pacote <code>@safe-access-inline/cli</code> fornece acesso via linha de comando a todas as funcionalidades da biblioteca:</p><table tabindex="0"><thead><tr><th>Comando</th><th>Descrição</th></tr></thead><tbody><tr><td><code>get</code></td><td>Ler um valor por caminho</td></tr><tr><td><code>set</code></td><td>Definir um valor em um caminho</td></tr><tr><td><code>remove</code></td><td>Remover um valor em um caminho</td></tr><tr><td><code>transform</code></td><td>Converter entre formatos (JSON ↔ YAML ↔ TOML)</td></tr><tr><td><code>diff</code></td><td>Gerar diff JSON Patch entre dois arquivos</td></tr><tr><td><code>mask</code></td><td>Ocultar valores sensíveis</td></tr><tr><td><code>layer</code></td><td>Mesclar múltiplos arquivos de configuração</td></tr><tr><td><code>keys</code></td><td>Listar chaves em um caminho</td></tr><tr><td><code>type</code></td><td>Mostrar o tipo de um valor em um caminho</td></tr><tr><td><code>has</code></td><td>Verificar existência de caminho (exit code 0/1)</td></tr><tr><td><code>count</code></td><td>Contar elementos em um caminho</td></tr></tbody></table><p>Suporta entrada via stdin (<code>-</code>), todos os formatos (JSON, YAML, TOML, XML, INI, CSV, ENV, NDJSON), saída formatada e expressões de caminho.</p><h2 id="integracoes-com-frameworks" tabindex="-1">Integrações com Frameworks <a class="header-anchor" href="#integracoes-com-frameworks" aria-label="Permalink to &quot;Integrações com Frameworks&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Framework</th><th>Pacote</th><th>Módulo</th><th>Funcionalidades</th></tr></thead><tbody><tr><td>NestJS</td><td>JS</td><td><code>SafeAccessModule</code></td><td>Módulo dinâmico, token de injeção <code>SAFE_ACCESS</code></td></tr><tr><td>Vite</td><td>JS</td><td><code>safeAccessPlugin</code></td><td>Módulo virtual, suporte HMR, merge de múltiplos arquivos</td></tr><tr><td>Laravel</td><td>PHP</td><td><code>LaravelServiceProvider</code></td><td>Binding singleton, wrapping do config repository</td></tr><tr><td>Symfony</td><td>PHP</td><td><code>SymfonyIntegration</code></td><td>Wrapping de ParameterBag, carregamento de arquivo YAML</td></tr></tbody></table><h2 id="registros-de-decisao-de-arquitetura" tabindex="-1">Registros de Decisão de Arquitetura <a class="header-anchor" href="#registros-de-decisao-de-arquitetura" aria-label="Permalink to &quot;Registros de Decisão de Arquitetura&quot;">​</a></h2><h3 id="adr-1-set-remove-usam-clone-em-vez-de-static-from" tabindex="-1">ADR-1: <code>set()</code> / <code>remove()</code> usam <code>clone</code> em vez de <code>static::from()</code> <a class="header-anchor" href="#adr-1-set-remove-usam-clone-em-vez-de-static-from" aria-label="Permalink to &quot;ADR-1: \`set()\` / \`remove()\` usam \`clone\` em vez de \`static::from()\`&quot;">​</a></h3><p><strong>Contexto:</strong> O PLAN.md especifica <code>set()</code> retorna <code>static::from($newData)</code>. No entanto, alguns accessors carregam metadata além do array normalizado — por exemplo, <code>XmlAccessor</code> armazena a string <code>originalXml</code>.</p><p><strong>Decisão:</strong> Tanto PHP quanto JS usam <code>clone</code> (PHP: <code>clone $this</code>; JS: método <code>clone(newData)</code>) para preservar qualquer metadata específica do accessor ao produzir uma nova instância. Apenas <code>$data</code> é atualizado.</p><p><strong>Consequência:</strong> Metadata como <code>originalXml</code> sobrevive a mutações, que é o comportamento esperado. O round-trip <code>set() → toXml()</code> ainda pode acessar o XML original via <code>getOriginalXml()</code>.</p><h3 id="adr-2-js-toxml-toyaml-totoml-via-bibliotecas-reais-plugin-override" tabindex="-1">ADR-2: JS <code>toXml()</code> / <code>toYaml()</code> / <code>toToml()</code> via Bibliotecas Reais + Plugin Override <a class="header-anchor" href="#adr-2-js-toxml-toyaml-totoml-via-bibliotecas-reais-plugin-override" aria-label="Permalink to &quot;ADR-2: JS \`toXml()\` / \`toYaml()\` / \`toToml()\` via Bibliotecas Reais + Plugin Override&quot;">​</a></h3><p><strong>Contexto:</strong> Inicialmente, o pacote JS omitia <code>toXml()</code> e <code>toYaml()</code> porque JavaScript não tem emissor XML nativo e serialização YAML exigiria uma dependência de runtime.</p><p><strong>Decisão (original):</strong> JS expunha apenas <code>toArray()</code>, <code>toJson()</code> e <code>toObject()</code>.</p><p><strong>Decisão (revisão #1):</strong> Com a introdução do Sistema de Plugins, JS expôs <code>toYaml()</code>, <code>toXml()</code> e <code>transform()</code> via PluginRegistry.</p><p><strong>Decisão (revisão #2):</strong> <code>js-yaml</code> e <code>smol-toml</code> agora são <code>dependencies</code> reais. <code>toYaml()</code> e <code>toToml()</code> funcionam sem configuração usando essas bibliotecas. Plugins fornecem override opcional para usuários que precisam de bibliotecas diferentes. <code>toXml()</code> ainda requer plugin.</p><p><strong>Consequência:</strong> Serialização YAML e TOML funciona sem configuração. Usuários que precisam de serializers alternativos registram um plugin override. Serialização XML ainda requer registro explícito de plugin.</p><h3 id="adr-3-dependencias-reais-para-yaml-toml-pluginregistry-para-override" tabindex="-1">ADR-3: Dependências Reais para YAML/TOML + PluginRegistry para Override <a class="header-anchor" href="#adr-3-dependencias-reais-para-yaml-toml-pluginregistry-para-override" aria-label="Permalink to &quot;ADR-3: Dependências Reais para YAML/TOML + PluginRegistry para Override&quot;">​</a></h3><p><strong>Contexto:</strong> Os accessors YAML e TOML do PHP originalmente usavam checks <code>class_exists()</code> e <code>function_exists()</code> para detectar parsers disponíveis em runtime. Depois, um PluginRegistry foi introduzido que exigia registro manual de plugins. PHP e JS tinham abordagens diferentes: PHP exigia plugins, JS tinha parsers leves embutidos.</p><p><strong>Decisão:</strong> Tornar bibliotecas YAML/TOML dependências reais em ambas plataformas. <code>js-yaml</code> + <code>smol-toml</code> são <code>dependencies</code> em JS. <code>symfony/yaml</code> + <code>devium/toml</code> são <code>require</code> em PHP. PluginRegistry continua existindo para override: se um plugin é registrado, ele tem prioridade sobre a biblioteca padrão.</p><p><strong>Escolhas-chave de design:</strong></p><ul><li><strong>Ambas plataformas</strong>: Parsing e serialização YAML/TOML funcionam sem configuração — zero configuração necessária.</li><li><strong>Plugin override</strong>: <code>PluginRegistry.registerParser()</code> / <code>PluginRegistry.registerSerializer()</code> ainda funciona — plugins registrados têm prioridade sobre bibliotecas padrão.</li><li><strong>Parsers embutidos JS removidos</strong>: Os antigos parsers leves YAML/TOML foram removidos em favor de bibliotecas comprovadas (<code>js-yaml</code>, <code>smol-toml</code>).</li><li><strong>Tipos de exceção</strong>: <code>InvalidFormatException</code> no nível do accessor, <code>UnsupportedTypeException</code> no nível do registro.</li><li><strong>Testes</strong>: Testes unitários usam plugins mock (classes/objetos anônimos) para isolamento. Testes de integração usam bibliotecas reais.</li></ul><p><strong>Consequência:</strong> Zero configuração para YAML/TOML em ambas plataformas. Comportamento consistente entre PHP e JS. Usuários que precisam de parsers/serializers alternativos os registram via PluginRegistry.</p>`,76)])])}const g=e(i,[["render",r]]);export{u as __pageData,g as default};
